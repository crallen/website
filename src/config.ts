// ── Feature flags ────────────────────────────────────────────────
// Set BLOG_ENABLED to true when the blog is ready to go live.
// This controls the nav link, footer RSS link, and whether the
// blog pages are built and included in the sitemap.

export const BLOG_ENABLED = true;
